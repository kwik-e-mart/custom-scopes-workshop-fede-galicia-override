apiVersion: v1
kind: Secret
metadata:
  name: appdynamics-access-key
  namespace: nullplatform
type: Opaque
# Placeholder content — solo para probar que la env var
# APPDYNAMICS_AGENT_ACCOUNT_ACCESS_KEY se resuelva vía secretKeyRef.
# NO es el access key real. La key del Secret debe llamarse igual que la env var,
# porque el wiring hace secretKeyRef.key = <nombre de la variable>.
stringData:
  APPDYNAMICS_AGENT_ACCOUNT_ACCESS_KEY: "placeholder-access-key"
