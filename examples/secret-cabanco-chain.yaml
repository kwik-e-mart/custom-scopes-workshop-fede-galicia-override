apiVersion: v1
kind: Secret
metadata:
  name: secret-cabanco-chain
  namespace: nullplatform
type: Opaque
# Placeholder content — solo para que el volume monte. No es un certificado real.
stringData:
  ca-chain.pem: "placeholder-cabanco-chain"
